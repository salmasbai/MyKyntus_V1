import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <div style="padding: 16px">
      <h1>Parrainage (Phase 1)</h1>
      <p>Angular scaffold + API client services (backend in-memory).</p>
    </div>
  `,
})
export class AppComponent {}

